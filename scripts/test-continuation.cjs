const assert = require('node:assert/strict')
const { load } = require('./test-ts-loader.cjs')
const { buildMiniMaxWorkflow } = load('src/lib/workflow.ts')
const { continuationBeatSignature, continuationOutputName, continuationOutputStem, continuationPreviousAction, continuationTiming } = load('src/lib/continuation.ts')
const models = { fl2va: 'fl2va', ref2va: 'ref2va', fastH3: 'fast', textEncoder: 'clip', videoVae: 'video', audioVae: 'audio', fl2vLora: 'fl-lora', ref2vLora: 'ref-lora' }
const base = { mode: 'text', prompt: 'She walks to the window.', width: 768, height: 768, duration: 5, seed: 42, steps: 30, turbo: 'off', sampler: 'res_multistep', scheduler: 'simple', refImageSize: 'match', filenamePrefix: 'video/test', referenceImages: [], referenceVideos: [], referenceAudios: [] }
const captured = buildMiniMaxWorkflow({ ...base, latentCapture: { filenamePrefix: 'h3_context/job-1' } }, models, { images: [], videos: [], audios: [] })
assert.equal(captured['190'].class_type, 'MiniMaxH3SaveLatent')
assert.equal(captured['190'].inputs.filename_prefix, 'h3_context/job-1')
assert.deepEqual(Array.from(captured['190'].inputs.latent), ['15', 0])
const timing = continuationTiming(5, 22)
assert.equal(timing.requestedFrames, 120)
assert.equal(timing.renderFrames, 141)
assert.equal(timing.trimFrames, 22)
assert.equal(timing.deliveredFrames, 119)
assert.equal(timing.renderDuration, 141 / 24)
assert.equal(timing.deliveredDuration, 119 / 24)
const blendedTiming = continuationTiming(5, 22, 2)
const motion = buildMiniMaxWorkflow({ ...base, duration: blendedTiming.renderDuration, motionContext: { latentPath: 'h3_context/job-1_00001.safetensors', contextFrames: 22, blendFrames: 2, carryAudio: true }, continuationAssembly: { sourceVideo: { name: 'source.mp4' }, trimFrames: blendedTiming.trimFrames, blendFrames: 2, useMotionTrim: true }, latentCapture: { filenamePrefix: 'h3_context/job-2' } }, models, { images: [], videos: [], audios: [], source: { name: 'source.mp4' } })
assert.equal(motion['160'].class_type, 'MiniMaxH3LoadLatent')
assert.deepEqual(Array.from(motion['161'].inputs.prev_latent), ['160', 0])
assert.deepEqual(Array.from(motion['12'].inputs.conditioning), ['161', 0])
assert.deepEqual(Array.from(motion['171'].inputs.trim_frames), ['161', 1])
assert.deepEqual(Array.from(motion['172'].inputs.images_a), ['1701', 0])
assert.deepEqual(Array.from(motion['172'].inputs.audio_a), ['1701', 1])
assert.deepEqual(Array.from(motion['18'].inputs.images), ['172', 0])
assert.deepEqual(Array.from(motion['18'].inputs.audio), ['172', 1])
assert.equal(motion['190'].inputs.filename_prefix, 'h3_context/job-2')
const silentMotion = buildMiniMaxWorkflow({ ...base, duration: timing.renderDuration, motionContext: { latentPath: 'h3_context/job-1_00001.safetensors', contextFrames: 22, blendFrames: 0, carryAudio: false, suppressAudio: true }, continuationAssembly: { sourceVideo: { name: 'source.mp4' }, trimFrames: timing.trimFrames, blendFrames: 0, useMotionTrim: true } }, models, { images: [], videos: [], audios: [], source: { name: 'source.mp4' } })
assert.equal(silentMotion['161'].class_type, 'MiniMaxH3MotionContext')
assert.deepEqual(Array.from(silentMotion['160'].inputs.vae), ['3', 0])
assert.deepEqual(Array.from(silentMotion['161'].inputs.context_frames), ['160', 1])
assert.deepEqual(Array.from(silentMotion['161'].inputs.video_context_latent), ['160', 0])
assert.equal(silentMotion['161'].inputs.context_latent, undefined)
assert.equal(blendedTiming.trimFrames, 22)
assert.equal(blendedTiming.deliveredDuration, 117 / 24)
assert.equal(continuationTiming(15, 0).renderFrames, 362)
assert.equal(continuationTiming(15, 0).renderDuration, 15)
assert.ok(continuationTiming(15, 22).renderFrames > 362)
assert.throws(() => buildMiniMaxWorkflow({ ...base, continuationAssembly: { sourceVideo: { name: 'source.mp4' }, trimFrames: 0, blendFrames: 0 } }, models, { images: [], videos: [], audios: [] }), /source video upload/)
const frameTiming = continuationTiming(5, 0)
assert.equal(frameTiming.renderFrames, 124)
assert.equal(frameTiming.trimFrames, 0)
assert.equal(frameTiming.deliveredFrames, 124)
const frameAssembly = buildMiniMaxWorkflow({ ...base, mode: 'image', duration: frameTiming.renderDuration, continuationAssembly: { sourceVideo: { name: 'source.mp4' }, trimFrames: frameTiming.trimFrames, blendFrames: 0, useMotionTrim: false }, firstFrame: 'frame.png' }, models, { first: { name: 'frame.png' }, images: [], videos: [], audios: [], source: { name: 'source.mp4' } })
assert.equal(frameAssembly['171'].inputs.trim_frames, 0)
assert.deepEqual(Array.from(frameAssembly['18'].inputs.images), ['172', 0])
const beat1 = { id: 'one', name: 'Walk to Window', prompt: 'Walk to the window.', sourceMode: 'previous', replacements: {} }
const beat2 = { id: 'two', name: 'Look Outside', prompt: 'Look Outside', sourceMode: 'previous', replacements: {} }
const beat3 = { id: 'three', name: 'Turn to Door', prompt: 'Turn toward the door.', sourceMode: 'previous', replacements: {} }
const script = { videoName: 'Morning Scene', beats: [beat1, beat2, beat3] }
assert.notEqual(continuationBeatSignature(beat1, script.videoName, 'text:last'), continuationBeatSignature(beat1, script.videoName, 'reference:motion'))
assert.equal(continuationOutputStem(script, beat3), 'Morning-Scene__beat1-Walk-to-Window_beat2-Look-Outside_beat3-Turn-to-Door')
assert.equal(continuationOutputStem({ ...script, beats: [beat1, beat2, { ...beat3, sourceMode: 'original' }] }, { ...beat3, sourceMode: 'original' }), 'Morning-Scene__beat3-Turn-to-Door')
assert.equal(continuationOutputStem({ ...script, beats: [beat1, beat2, { ...beat3, sourceMode: 'beat', sourceBeatId: beat1.id }] }, { ...beat3, sourceMode: 'beat', sourceBeatId: beat1.id }), 'Morning-Scene__beat1-Walk-to-Window_beat3-Turn-to-Door')
assert.equal(continuationOutputName({ ...script, id: 'script-1' }, beat2), 'continuations/Morning-Scene/Morning-Scene__beat1-Walk-to-Window_beat2-Look-Outside')
assert.equal(continuationOutputName({ ...script, id: 'script-1' }, beat3), 'continuations/Morning-Scene/Final_Morning-Scene__beat1-Walk-to-Window_beat2-Look-Outside_beat3-Turn-to-Door')
assert.equal(continuationPreviousAction({ ...script, id: 'script-1' }, { continuityState: { scene: 'Original authored action.' } }), 'Original authored action.')
assert.equal(continuationPreviousAction({ ...script, id: 'script-1' }, { continuation: { scriptId: 'script-1', beatId: 'two' }, continuityState: { scene: 'Recursive compiled prompt that must not be reused.' } }), 'Look Outside')
assert.equal(continuationPreviousAction({ ...script, id: 'script-1' }, { continuation: { scriptId: 'older-script', beatId: 'two' }, continuityState: { scene: 'Old compiled continuation prompt.' } }), '')
console.log('PASS: H3 AV latent capture, motion guide, synchronized trim, joined output graph, and truthful branch filenames')
